# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Build / TS
tsconfig.tsbuildinfo
dist/
build/

# Env vars (contienen credenciales, nunca deben subirse).
# .env.example y .env.test sí viajan: no tienen secretos y son lo que permite
# que quien clone el repo levante su base sin pedirle credenciales a nadie.
.env*
!.env.example
!.env.test

# Payload local media (uploads locales de desarrollo)
media/

# Testing
/coverage
/playwright-report/
/test-results/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude
.claude
# Tickets y notas de trabajo locales (no viajan con el repo)
.scratch/
